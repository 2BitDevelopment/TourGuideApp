rules_version = '2'
service firebase.storage{
match /b/{bucket}/o{
    match/images/{imageID}{
        allow read: if true;
        allow write: if false
    }
    
    match/{allPaths=**}{
    allow read: if false;
    allow write: if false;
  }
 }
}